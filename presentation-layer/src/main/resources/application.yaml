spring:
    application:
      name: "base"
    profiles:
        include:
            - temporary
            - local
            - stage
            - production
    config:
        import: 'classpath:common.yaml'
