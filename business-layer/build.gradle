dependencies {
 implementation(project(":persistence-layer"))
 implementation(project(":common-layer"))
}

dependencies {
 testImplementation(project(":common-layer"))

}

test {
    useJUnitPlatform()
}
